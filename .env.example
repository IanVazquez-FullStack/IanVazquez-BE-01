PORT=3000
SUPABASE_URL=your_project_url
SUPABASE_KEY=your_anon_key
# DATABASE_URL=postgresql://user:password@localhost:5432/tasks
# STORAGE=memory
# SQLITE_DB_PATH=./tasks.db
# REPORT_STORAGE_DIR=./storage/reports

# LLM provider (OpenAI-compatible). Swap these three to change provider.
LLM_BASE_URL=
LLM_API_KEY=
LLM_MODEL=
# LLM_STUB=1        # skip the model and return a deterministic classification
# LLM_ENABLED=false # kill switch: return 503 without calling the model
